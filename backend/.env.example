# Database path (use forward slashes on Linux/Mac, backslashes on Windows)
# For cross-platform compatibility, use relative paths or __DIR__ in config
DB_PATH="./database/campus-teamup.db"

# App
APP_ENV=development
APP_DEBUG=true

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://your-domain.vercel.app